name: Pull Request Workflow

on:
  pull_request:
    branches:
    - main

jobs:
  enforce_git_lfs:
    name: Enforce Git LFS
    runs-on: ubuntu-latest
    steps:
    - name: LFS Check
      uses: MPLew-is/lfs-check-action@1.0.0
